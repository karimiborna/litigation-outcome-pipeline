# MLflow tracking server configuration
# Run locally: mlflow server --config mlflow/server_config.yaml
# Or just: mlflow server --backend-store-uri sqlite:///mlruns/mlflow.db --default-artifact-root mlruns/artifacts

backend_store_uri: "sqlite:///mlruns/mlflow.db"
default_artifact_root: "mlruns/artifacts"
host: "0.0.0.0"
port: 5000
